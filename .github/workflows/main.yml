name: Copy Notebooks to blob store shared container

on:
  push:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3
    
    - uses: azure/login@v1
      with:
        creds: ${{ secrets.AZURE_CREDENTIALS }}
   
    - name: Upload to blob storage
      uses: azure/CLI@v1
      env:
        AZURE_STORAGE_KEY: ${{ secrets.AZURE_STORAGE_KEY }}
      with:
        inlineScript: |
          myip=$(curl -s ipv4.icanhazip.com | tr -d '\n')
          az storage account network-rule add --resource-group AZURE_RESOURCE_GROUP --account-name AZURE_STORAGE_ACCOUNT --ip-address $myip
          az storage blob delete-batch --account-name livingwalesstorage --auth-mode login -s 'notebooks' 
          az storage blob upload-batch --account-name livingwalesstorage --auth-mode login -d 'notebooks' -s ./ 
          az storage account network-rule remove --resource-group AZURE_RESOURCE_GROUP --account-name AZURE_STORAGE_ACCOUNT --ip-address $myip

    - name: logout
      run: |
        az logout
      if: always()
